<Window x:Class="JsonBackend.Window.CreatePersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Person" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height" 
        Width="350">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <Label Grid.Row="0" Grid.Column="0" Content="Name:" VerticalAlignment="Center"/>
        <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Last Seen (Location) -->
        <Label Grid.Row="1" Grid.Column="0" Content="Last Seen:" VerticalAlignment="Center"/>
        <TextBox x:Name="LastSeenTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Social Credit -->
        <Label Grid.Row="2" Grid.Column="0" Content="Social Credit:" VerticalAlignment="Center"/>
        <TextBox x:Name="SocialCreditTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Create Button -->
        <Button x:Name="CreateButton" Grid.Row="3" Grid.Column="1" Content="Create" Width="100" Height="30" Margin="5" HorizontalAlignment="Left"/>
    </Grid>
</Window>